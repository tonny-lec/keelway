# Project conventions

Use the Python standard library only.
Tests use unittest with descriptive test names and run with `python3 -m unittest discover -s tests -v`.
For a public function behavior change, add a concise docstring describing the relevant error.
